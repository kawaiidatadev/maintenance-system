# Generar PDF de orden de trabajo
pdf = generate_work_order_pdf(work_order, template_name='work_order')

# Generar PDF de mantenimiento preventivo
pdf = generate_preventive_pdf(preventive_order, template_name='preventive')

# Generar PDF de inventario
pdf = generate_inventory_pdf(inventory_item, template_name='inventory')